ilo.publications Installation
-----------------------------

To install ilo.publications using zc.buildout and the plone.recipe.zope2instance
recipe to manage your project, you can do this:

* Add ``ilo.publications`` to the list of eggs to install, e.g.:

    [buildout]
    ...
    eggs =
        ...
        ilo.publications
       
* Re-run buildout, e.g. with:

    $ ./bin/buildout
